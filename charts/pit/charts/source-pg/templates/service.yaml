apiVersion: v1
kind: Service
metadata:
  name: {{ include "source-pg.fullname" . }}-headless
  labels:
    {{- include "source-pg.labels" . | nindent 4 }}
spec:
  # Required by the StatefulSet for stable per-pod DNS.
  clusterIP: None
  ports:
    - name: postgresql
      port: {{ .Values.service.port }}
      targetPort: postgresql
  selector:
    {{- include "source-pg.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "source-pg.fullname" . }}
  labels:
    {{- include "source-pg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: postgresql
      port: {{ .Values.service.port }}
      targetPort: postgresql
      protocol: TCP
  selector:
    {{- include "source-pg.selectorLabels" . | nindent 4 }}
